{
	"name": "genesis-workflow-action",
	"version": "1.0.0",
	"description": "GitHub Action to trigger Genesis workflows and retrieve results",
	"type": "module",
	"license": "MIT",
	"scripts": {
		"build": "tsc",
		"typecheck": "tsc --noEmit",
		"lint": "biome check",
		"lint:fix": "biome check --write",
		"test": "npm run build && node --test dist/tests/lib.test.js",
		"check": "npm run lint && npm run typecheck && npm run test"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.6",
		"@types/node": "^25.5.0",
		"typescript": "^5.9.3"
	}
}
